# AdsCompetitor Project Requirements - Production
# Note: Some dependencies are optional and the app will work without them

# AI Generation Layer Dependencies (works on Python 3.8+)
google-generativeai>=0.3.0

# Notification Layer Dependencies (optional - app works without them)
twilio>=8.0.0
sendgrid>=6.10.0

# Web Framework (required)
flask>=2.3.0
flask-cors>=4.0.0

# Firebase Authentication (optional - enables real login/signup)
firebase-admin>=6.0.0

# Stripe (required for paid plans - Starter/Pro)
stripe>=7.0.0

# Auto Marketing Mode (scheduled campaigns; pulls in tzlocal)
apscheduler>=3.10.0,<4

# For environment variable management
python-dotenv>=1.0.0

# For background job queue (optional - app has fallback)
# Uncomment if you need background jobs
# redis>=5.0.0
# rq>=1.15.0

# For PostgreSQL database (optional - app works without it)
# Uncomment if you need database persistence
# psycopg2-binary>=2.9.0

# For competitor intelligence scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
googlesearch-python>=1.2.3
lxml>=4.9.0
